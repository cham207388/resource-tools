# Podman

## installation
`brew install podman`
`brew install podman-desktop`

## Start

`podman machine init`
`podman machine start`

Runs as nonroot

To run as root, execute `podman machine set --rootful`
